import { ComponentFixture, TestBed } from '@angular/core/testing';
import { NumbersdirectoryshopPage } from './numbersdirectoryshop.page';

describe('NumbersdirectoryshopPage', () => {
  let component: NumbersdirectoryshopPage;
  let fixture: ComponentFixture<NumbersdirectoryshopPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(NumbersdirectoryshopPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
